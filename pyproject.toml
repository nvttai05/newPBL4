[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sandbox"
version = "0.1.0"
dependencies = [
  "fastapi",
  "uvicorn",
  "sqlmodel",
  "pydantic>=2",
  "pyyaml",
  "structlog",
]

[tool.setuptools.packages.find]
where = ["src"]
